import state from "../app/store/state"
import * as mutations from "../app/store/mutations"
import * as action from "../app/store/mutations"
import * as getters from "../app/store/getters"

export default 
    {
        namespaced:true,
        mutations,
        action,
        getters,
}
